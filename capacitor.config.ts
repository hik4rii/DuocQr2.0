import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.duoc.app',
  appName: 'RegistApp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
